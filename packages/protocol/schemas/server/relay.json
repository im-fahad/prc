{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://prc.local/schemas/server/relay.json",
  "type": "object",
  "required": ["kind", "to", "envelope"],
  "properties": {
    "kind": { "const": "relay" },
    "to": { "$ref": "https://prc.local/schemas/common.json#/$defs/deviceId" },
    "envelope": { "$ref": "https://prc.local/schemas/envelope.json" }
  }
}
